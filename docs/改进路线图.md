# 改进路线图（Roadmap）

> 更新：2026-09-11。承接 `docs/Token控制改进方案.md` 的调研结论，将全项目可改进方向收敛为一份可执行清单。
> 每条给出目标、优先级与验收方式；已完成的条目在 CHANGELOG 中留痕。

## P0 — 性能与成本

| # | 方向 | 现状 | 行动 | 验收 |
|---|------|------|------|------|
| 1 | 统一系统提示词前缀（Token 策略 2B） | ✅ 已完成（68f7d0f） | — | guard tests 通过 |
| 2 | 分层模型路由（Token 策略 1） | ✅ 节点级路由已存在：辩手→heavy、研究经理+交易员+风控→standard、分析师→quick、基金经理→deep；信号提取为规则引擎（0 Token） | 剩余：`TASK_COMPLEXITY_MAP` 显式常量化供文档引用 | 映射常量 + 文档一致 |
| 3 | 摘要式上下文传递（Token 策略 2A） | ✅ 研究员路径完成：`_compress_prose()` 对超长报告做确定性摘要（保留标题/列表/含数值行，长段只留首句，0 额外 token） | 剩余：基金经理侧矩阵化输出 | `tests/test_context_digest.py` |
| 4 | 语义缓存（Token 策略 3） | ✅ `llm_clients/semantic_cache.py`（TTL+LRU+归一化key+0.92 相似度），默认关闭挂在 `_safe_invoke` | 复现实验：同股同日重复分析命中率统计 |

## P1 — 能力边界

| # | 方向 | 说明 |
|---|------|------|
| 5 | **MCP server 化** | ✅ 零依赖 `mcp_server.py`：analyze_stock / list_snapshots / get_snapshot / read_recent_memories，stdio NDJSON 协议含端到端测试 |
| 6 | **外部校准接入** | 跟踪可公开结算、与 A 股个股评级可比的第三方记分板（已知候选：Headline Arena，见 issue #1；当前其赛道为宏观期货/Civic Index，题域错位暂不接入）。接入原则：只镜像最终评级与置信度，不镜像内部记忆 |
| 7 | 回测复盘 MCP 工具 | 在 `mcp_server.py` 增加 `review_backtest` 工具，复用 scripts/review_backtest.py |
| 8 | 数据源容灾 | akshare 失败时自动降级 tushare/mx（已有实现雏形，需加降级顺序测试） |

## P2 — 生态与体验

| # | 方向 | 说明 |
|---|------|------|
| 9 | Skills 扩展 | 行业对比分析、龙虎榜解读、同板块轮动对比三个候选技能 |
| 10 | CI 工作流 | ✅ `.github/workflows/ci.yml` 已存在（ruff + pytest 矩阵 3.10/3.11/3.12） |
| 11 | 国际化 | README 英文精简版 + 术语表（买入/增持/持有/减持/卖出 ↔ buy/overweight/hold/reduce/sell） |
| 12 | 文档站 | `docs/` 折叠整理 + GitHub Pages |

## 已搁置/不做

- Headline Arena 即刻接入（题域错位、数据出境依赖；跟踪中）——issue #1
